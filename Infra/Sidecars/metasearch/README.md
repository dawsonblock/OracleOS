# Oracle Metasearch Sidecar — SearXNG configuration
#
# Deploy SearXNG here. Oracle calls POST /search.
#
# Quick start:
#   docker run -d --name metasearch -p 8084:8888 searxng/searxng
#
# Or use the docker-compose below.

# This file is a placeholder — actual SearXNG configuration
# goes in settings.yml per SearXNG docs.

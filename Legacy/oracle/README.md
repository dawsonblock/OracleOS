# Legacy oracle

This tree is isolated for review because it risks duplicating the runtime.
It should either be deleted later or extracted into tooling, but it must not remain part of the active runtime boundary.
